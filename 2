#include <stdio.h>
#include <string.h>
int main () {
char word[] = "yellow";
char substring[4];
int start=3;
int lenght = 3;
if( start>=0 && start < strlen(word) && length > 0 && (start + length) <= strlen(word) {
for (int i = 0; i<length; i++) {
substring[i] = word[start + i];
}
substring[length] = '\0';
printf("Исходная строка: %s\n", word);
printf("Подстрока: %s\n", substring);
} else {
printf("Невозможно выделить подстроку с такими параметрами\n");
}
return 0;
}
